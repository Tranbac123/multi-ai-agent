[pytest]
addopts = -q
testpaths = apps
markers =
    unit: Unit tests
    contract: Contract/schema tests
    integration: Lightweight integration (no heavy external deps)
    p0: Must-pass smoke for every PR (subset)
filterwarnings =
    ignore::DeprecationWarning
