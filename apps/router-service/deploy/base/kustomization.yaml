apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: router-service-base

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - serviceaccount.yaml
  - hpa.yaml

commonLabels:
  app: router-service
  component: microservice
  version: v1