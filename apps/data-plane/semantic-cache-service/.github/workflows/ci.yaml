name: semantic-cache-service-ci

on:
  push:
    branches: [main]
    paths: ["apps/data-plane/semantic-cache-service/**"]
  pull_request:
    paths: ["apps/data-plane/semantic-cache-service/**"]

jobs:
  call:
    uses: ./platform/ci-templates/python-service-ci.yaml
    with:
      service_path: apps/data-plane/semantic-cache-service
      image_name: semantic-cache-service

