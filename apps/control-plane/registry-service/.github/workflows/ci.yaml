name: registry-service-ci
on:
  push: { branches: [main], paths: ["apps/control-plane/registry-service/**"] }
  pull_request: { paths: ["apps/control-plane/registry-service/**"] }
jobs:
  call:
    uses: ./platform/ci-templates/python-service-ci.yaml
    with:
      service_path: apps/control-plane/registry-service
      image_name: registry-service

