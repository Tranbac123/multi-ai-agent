name: "Chat Adapters CI/CD"

on:
  push:
    paths:
      - "apps/chat-adapters/**"
      - "libs/**"
      - "contracts/**"
      - "platform/ci-templates/**"
      - ".github/workflows/chat-adapters-*.yaml"
  pull_request:
    paths:
      - "apps/chat-adapters/**"
      - "libs/**"
      - "contracts/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: "Build and Test Chat Adapters"
    uses: ./platform/ci-templates/service-ci.yaml
    with:
      language: "python"
      service_path: "apps/chat-adapters"
      service_name: "chat-adapters"
      enable_docker: true
      enable_sbom: true
    secrets:
      DOCKER_REGISTRY_URL: ${{ secrets.DOCKER_REGISTRY_URL }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

  # Optional: Service-specific deployment to dev environment
  deploy-dev:
    name: "Deploy to Development"
    needs: ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to development
        run: |
          echo "Deploying chat-adapters to development environment"
          # Add your deployment logic here
          # cd apps/chat-adapters/deploy
          # make deploy ENV=dev IMAGE_TAG=${{ github.sha }}
