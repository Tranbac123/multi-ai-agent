apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: chat-adapters-dev

namespace: multi-ai-agent-dev

resources:
  - ../../base

patchesStrategicMerge:
  - deployment-patch.yaml
  - configmap-patch.yaml

configMapGenerator:
  - name: chat-adapters-config
    behavior: merge
    literals:
      - ENVIRONMENT=dev
      - LOG_LEVEL=DEBUG