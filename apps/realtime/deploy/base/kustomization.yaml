apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: realtime-base

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - serviceaccount.yaml
  - hpa.yaml

commonLabels:
  app: realtime
  component: microservice
  version: v1